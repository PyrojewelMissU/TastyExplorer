# 🍽️ 美味探索家 (TastyExplorer)

<div align="center">

**一个现代化的美食菜单Web应用 | 智能翻译 · 批量导入 · 离线缓存**

[![Vue](https://img.shields.io/badge/Vue-3.3+-green.svg)](https://vuejs.org/)
[![Vite](https://img.shields.io/badge/Vite-5.0+-purple.svg)](https://vitejs.dev/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

</div>

---

## 📖 项目简介

**美味探索家**是一个基于 Vue 3 的现代化美食菜单应用,专为移动端优化。通过调用 TheMealDB API 获取全球美食数据,并集成免费翻译 API 实现**中英双语**展示,让用户能够轻松探索世界各地的美味佳肴。

### ✨ 核心亮点

- 🌍 **200+ 国际菜品** - 批量导入功能,一键获取丰富菜单
- 🔄 **智能翻译系统** - 自动将英文菜品、配料、制作步骤翻译成中文
- 💾 **离线缓存机制** - LocalStorage 本地缓存,快速加载
- 📱 **移动端优先** - 响应式设计,完美适配各种设备
- ❤️ **收藏管理** - 跨页面实时同步收藏状态
- 🎨 **精美动画** - 流畅的交互动画,提升用户体验

---

## 🚀 快速开始

### 环境要求
- Node.js 16+
- npm 或 yarn 包管理器

### 安装步骤

```bash
# 1. 进入项目目录
cd tasty-explorer

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 浏览器访问 http://localhost:3000
```

### 生产构建

```bash
# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

---

## 🎯 核心功能

### 1. 📋 美食列表 (首页)
- ✅ 展示 200+ 国际美食卡片
- ✅ 菜品名称自动翻译为中文
- ✅ 一键收藏/取消收藏
- ✅ 菜品统计徽章显示
- ✅ 优雅的加载动画
- ✅ 错误重试机制

### 2. 📖 美食详情页
- ✅ 高清美食大图展示
- ✅ 菜品名称、分类、地区标签(中文)
- ✅ **所需配料列表**(中英对照)
- ✅ **制作步骤**(智能翻译成中文)
- ✅ 收藏功能 + Toast 提示
- ✅ 返回导航按钮

### 3. ❤️ 收藏管理页
- ✅ 展示所有收藏的美食
- ✅ 空状态友好提示
- ✅ 实时同步收藏变化
- ✅ 可直接取消收藏

### 4. 🔄 批量导入菜单
- ✅ 底部导航栏"导入菜单"按钮
- ✅ 一键批量导入 200 道菜品
- ✅ 并行 API 请求,速度快
- ✅ 自动去重和随机排序
- ✅ 智能缓存,避免重复加载

### 5. 🌐 智能翻译系统
- ✅ **本地翻译字典**(200+ 菜品名,300+ 配料)
- ✅ **双重 API 翻译**: MyMemory + LibreTranslate
- ✅ **翻译缓存机制** - 避免重复请求
- ✅ **降级策略** - API失败时保留英文
- ✅ **长文本分句翻译** - 提高成功率

### 6. 💾 智能缓存机制
- ✅ 首次访问自动批量导入并缓存
- ✅ 再次访问瞬间加载(从缓存)
- ✅ 手动刷新获取新菜单
- ✅ 控制台日志显示缓存状态

---

## 🏗️ 技术架构

### 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue 3** | 3.3+ | 前端框架,Composition API |
| **Vue Router** | 4.2+ | SPA 路由管理(Hash 模式) |
| **Vite** | 5.0+ | 构建工具,快速开发体验 |
| **Axios** | 1.6+ | HTTP 请求库 |
| **HTML5** | - | 语义化标签 |
| **CSS3** | - | Flexbox、动画、媒体查询 |
| **ES6+** | - | 异步编程、模块化 |

### 外部 API

| API | 用途 | 说明 |
|-----|------|------|
| **TheMealDB API** | 获取美食数据 | 免费,丰富的国际美食资源 |
| **MyMemory Translation API** | 文本翻译(主要) | 1000次/天,无需密钥 |
| **LibreTranslate API** | 文本翻译(备用) | 开源免费,无限制 |

---

## 📂 项目结构

```
tasty-explorer/
├── index.html                 # HTML 入口
├── package.json               # 项目配置
├── vite.config.js             # Vite 构建配置
├── README.md                  # 项目文档
│
├── src/
│   ├── main.js                # 应用入口
│   ├── App.vue                # 根组件
│   │
│   ├── assets/
│   │   └── style.css          # 全局样式
│   │
│   ├── api/
│   │   └── food.js            # 🔥 API接口(批量导入+缓存)
│   │
│   ├── utils/
│   │   ├── translate.js       # 🔥 本地翻译字典(500+条目)
│   │   └── translationAPI.js  # 🔥 翻译API调用(双重备份)
│   │
│   ├── router/
│   │   └── index.js           # 路由配置
│   │
│   ├── components/
│   │   ├── FoodCard.vue       # 美食卡片组件
│   │   └── TabBar.vue         # 🔥 底部导航栏(3个标签)
│   │
│   └── views/
│       ├── Home.vue           # 🔥 首页(批量导入逻辑)
│       ├── Detail.vue         # 🔥 详情页(完整翻译)
│       └── Favorite.vue       # 收藏页
```

---

## 🔧 核心技术实现

### 1. 批量导入策略

```javascript
// src/api/food.js
const fetchFoodListFromAPI = async () => {
  // 按首字母并行请求 20 个接口
  const letters = ['a', 'b', 'c', ...];
  const requests = letters.map(letter =>
    axios.get(`/search.php?f=${letter}`)
  );

  // 等待所有请求完成,去重、随机排序
  const results = await Promise.all(requests);
  // 返回前 200 个菜品
}
```

**优势**: 并行请求,速度快 | 自动去重 | 随机排序 | 控制数量

### 2. 智能翻译系统

```javascript
// src/utils/translate.js
export const translateMealName = async (mealName) => {
  // 1. 优先查本地字典
  if (mealTranslations[mealName]) {
    return mealTranslations[mealName];
  }

  // 2. 调用翻译 API
  const translated = await translateText(mealName);
  return translated || mealName; // 失败返回原文
};
```

**特点**: 本地优先 | API降级 | 翻译缓存 | 长文本分句

### 3. 离线缓存机制

```javascript
// src/api/food.js
export const getFoodList = async (forceRefresh = false) => {
  // 非强制刷新时,优先读缓存
  if (!forceRefresh) {
    const cached = localStorage.getItem('foodMenuCache');
    if (cached) return JSON.parse(cached);
  }

  // 获取新数据并缓存
  const data = await fetchFoodListFromAPI();
  localStorage.setItem('foodMenuCache', JSON.stringify(data));
  return data;
};
```

**优势**: 首次自动缓存 | 瞬间加载 | 手动刷新 | 减少请求

### 4. 跨组件状态同步

```javascript
// 触发收藏变化
localStorage.setItem('favorites', JSON.stringify(favorites));
window.dispatchEvent(new Event('storage'));

// 监听收藏变化
window.addEventListener('storage', loadFavorites);
```

---

## 🎓 技术难点和解决方案

### 难点 1: 大量菜品的批量获取

**问题**: API 单次查询只返回约 60 个菜品

**解决方案**:
- 使用 `search.php?f={letter}` 按首字母搜索
- 并行请求 20 个首字母(a-z)
- Map 去重,获得 150-250 个不重复菜品
- 随机排序后取前 200 个

### 难点 2: 菜品和配料的翻译

**问题**: API 返回英文,翻译 API 有限制

**解决方案**:
1. 本地翻译字典(500+ 条目)
2. 双重 API 备份(MyMemory + LibreTranslate)
3. 翻译缓存避免重复请求
4. 降级策略保留英文

### 难点 3: 制作步骤长文本翻译

**问题**: 长文本(500-1000字)翻译易超时

**解决方案**:
- 检测长度,超过 500 字符启用分句翻译
- 按标点符号分割句子
- 逐句翻译,失败的句子保留英文
- 拼接所有翻译结果

### 难点 4: 配料数据整合

**问题**: 配料分散在 20 个字段(strIngredient1~20)

**解决方案**:
```javascript
for (let i = 1; i <= 20; i++) {
  const ingredient = food[`strIngredient${i}`];
  if (ingredient && ingredient.trim()) {
    list.push({
      name: await translateIngredient(ingredient),
      measure: food[`strMeasure${i}`]
    });
  }
}
```

### 难点 5: 跨组件收藏同步

**问题**: 详情页收藏后,列表页需实时更新

**解决方案**: 使用 localStorage + 自定义事件

---

## 📊 数据流程图

```
用户访问首页
    ↓
检查缓存
    ├── 有缓存 → 从 localStorage 加载 → 显示菜品
    └── 无缓存 → 批量导入
                    ↓
            并行请求 20 个首字母
                    ↓
            合并、去重、随机排序
                    ↓
            保存到缓存 → 显示菜品
                    ↓
            异步翻译菜品名称

用户点击"导入菜单"
    ↓
清除缓存 → 重新批量导入 → 显示新菜品

用户点击菜品卡片
    ↓
跳转详情页 → 并行翻译(名称、分类、配料、步骤)
```

---

## 🌟 项目特色

| 特色 | 说明 |
|------|------|
| 🌍 **国际化** | 英文数据 + 中文翻译,双语展示 |
| ⚡ **高性能** | 批量导入 + 智能缓存 + 并行请求 |
| 📱 **移动优先** | 专为手机优化,触摸友好 |
| 🎨 **精美UI** | 零外部UI库,纯手工打造 |
| 💾 **离线友好** | LocalStorage 缓存,快速加载 |
| 🔄 **智能翻译** | 本地字典 + 双重API + 缓存 |

---

## 🔍 浏览器兼容性

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ iOS Safari 14+
- ✅ Chrome Mobile

---

## 📈 未来改进方向

- [ ] 添加美食搜索功能
- [ ] 添加分类/地区筛选
- [ ] 支持 PWA(离线访问)
- [ ] 添加暗黑模式
- [ ] 购物清单功能
- [ ] 图片懒加载优化
- [ ] 分享功能
- [ ] 浏览历史记录

---

## 📝 项目信息

- **项目名称**: 美味探索家 (TastyExplorer)
- **版本**: v2.0.0
- **作者**: Web 前端课程期末作品
- **技术栈**: Vue 3 + Vite + ES6+
- **许可证**: MIT License

---

## 🙏 致谢

- **TheMealDB API** - 提供丰富的美食数据
- **MyMemory Translation API** - 提供免费翻译服务
- **LibreTranslate** - 开源翻译解决方案
- **Vue.js 社区** - 优秀的前端框架

---

<div align="center">

**Made with ❤️ and Vue 3**

⭐ 如果这个项目对你有帮助,请给个 Star!

</div>
